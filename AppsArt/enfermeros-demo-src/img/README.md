Poné acá el logo real cuando esté listo: `logo.png` o `logo.svg`, cuadrado
(mismo ancho que alto, para que quede bien como ícono redondeado).

Cuando esté el archivo, falta:

- Reemplazar el ícono de emoji (♥ / ⚕ / 🔒) por el logo real en las 4 pantallas
  (`index.html`, `paciente.html`, `enfermero.html`, `admin.html`).
- Sacar el banner "MODO DEMO" de los 4 HTML.
- Limpiar `demoBannerHTML()` en `firebase-init.js`, que queda sin uso una vez
  sacado el banner de los 4 HTML.

Nota: esta carpeta no persiste sola en git si queda vacía (git no trackea
carpetas vacías) — por eso este README hace de placeholder hasta que haya
un logo real adentro.
